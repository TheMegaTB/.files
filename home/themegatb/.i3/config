# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $main   DP-4
set $aux    HDMI-0
set $side   HDMI-0

set $mod Mod4

set $red		#fb4934
set $green		#4E9C00
set $dark_green		#1B3600
set $white		#EBDBB2
set $transparent	#333341A0

set $ws1  "1:  "
set $ws2  "2:  "
set $ws3  "3:  "
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8: "
set $ws9  "9: "

workspace $ws1 output $aux
workspace $ws2 output $main
workspace $ws3 output $main
workspace $ws4 output $aux
workspace $ws8 output $aux
workspace $ws9 output $side

# Gaps between windows
gaps inner 10
gaps outer 0

workspace $ws1 gaps inner 0
workspace $ws1 gaps outer 10

smart_borders on

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8
font pango:Hack, FontAwesome 10

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+space exec --no-startup-id rofi -show run
bindsym $mod+Tab exec --no-startup-id rofi -show window

# switch workspaces by cursor keys
# bindsym $mod+Left workspace prev
# bindsym $mod+Right workspace next
# bindsym $mod+Up exec --no-startup-id ~/.i3/empty_workspace.sh
# bindsym $mod+Down exec --no-startup-id rofi -show run

# # change focus
# bindsym $mod+j focus left
# bindsym $mod+k focus down
# bindsym $mod+l focus up
# bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
#bindsym $mod+h split h

# split in vertical orientation
#bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

set $mode_resize <span>        </span><span background='#fabd2f' foreground='#000000'> resize </span> <span foreground='#FFFFFF'>l</span>eft <span foreground='#FFFFFF'>r</span>ight <span foreground='#FFFFFF'>u</span>p <span foreground='#FFFFFF'>d</span>own
bindsym $mod+r mode "$mode_resize"
mode --pango_markup "$mode_resize" {
	bindsym Left resize shrink width 10 px or 1 ppt
	bindsym Down resize grow height 10 px or 1 ppt
	bindsym Up resize shrink height 10 px or 1 ppt
	bindsym Right resize grow width 10 px or 1 ppt

	bindsym Shift+Left resize shrink width 20 px or 5 ppt
	bindsym Shift+Down resize grow height 20 px or 5 ppt
	bindsym Shift+Up resize shrink height 20 px or 5 ppt
	bindsym Shift+Right resize grow width 20 px or 5 ppt

	bindsym Return mode "default"
	bindsym Escape mode "default"
}

# Generic settings
workspace_auto_back_and_forth yes

# App definition
set $chromePop  [window_role=$WR_popup class="google-chrome"]
set $gcal       [window_role="browser-window" title="Google Calendar(?i)"]
set $inbox      [window_role="browser-window" title="(?i)Inbox"]
set $gkeep      [window_role="browser-window" title="Google Keep"]
set $gdrive     [window_role="browser-window" title="(?i)Google Drive"]
set $spotify    [class="Spotify"]
set $discord    [class="discord"]
set $whatsapp   [window_role="pop-up" title="(?i)WhatsApp"]
set $hangouts   [title="Google Hangouts(?i)"]
set $resolve_sec [class="resolve" title="Secondary Screen"]

# Put specific windows in floating mode
for_window $chromePop floating enable, move position center
for_window [class="Nautilus" instance="file_progress"] floating enable
for_window [class="(?i)pavucontrol"] floating enable, move position center
for_window [class="(?i)Splashscreen"] floating enable
for_window [class="net-ftb-main-Main"] floating enable
for_window [title="Complete Installation"] floating enable
for_window [class="Cerebro"] floating enable
for_window [title="(?i)POPUP(?i)"] floating enable

# Bind specific windows to specific workspaces
assign [title="Soundnode(?i)"]	$ws9
assign [class="google-chrome-beta" window_role="^((?!pop-up).)*$"]	$ws1
assign [class="Firefox"] $ws1
assign [class="Eclipse"] $ws3
assign [class="(?i)atom"]	$ws3
assign [class="(?i)PyCharm(?i)"] $ws3
assign [class="(?i)WebStorm(?i)"] $ws3
assign [class="(?i)CLion(?i)"] $ws3
assign [class="qemu-system-x86_64"] $ws8
assign $resolve_sec $ws4

# Set properties for specific windows
for_window [window_role="pop-up"] floating enable
for_window $resolve_sec floating disable
for_window $discord     resize set 1400 1000, move position center, move to scratchpad, scratchpad show
for_window $spotify     move to workspace $ws9
for_window $whatsapp    resize set 700 700, move position center, move to scratchpad, scratchpad show
for_window $hangouts    move to scratchpad, scratchpad show
# Electron page apps
for_window [class="Electron"] floating enable, move position center
for_window $gcal        resize set 1700 780, move position center, move to scratchpad, scratchpad show
for_window $inbox       resize set 1000 1000, move position center, move to scratchpad, scratchpad show
for_window $gkeep       resize set 1500 1000, move position center, move to scratchpad, scratchpad show
for_window $gdrive      resize set 1000 1000, move position center, move to scratchpad, scratchpad show

# Custom keybindings
bindsym $mod+shift+a exec --no-startup-id ~/.scripts/audio/circleSinks.sh
bindsym $mod+shift+f fullscreen toggle global
bindsym $mod+control+l exec ~/.i3/i3-fancy-lock/lock
bindsym $mod+shift+s exec --no-startup-id teiler
bindsym $mod+p exec --no-startup-id rofi-pass
bindsym $mod+y fullscreen disable; floating enable; resize set 886 500; sticky enable; move window to position center

## Navigation
bindsym $mod+shift+comma workspace next
bindsym $mod+shift+m workspace prev
bindsym $mod+o move scratchpad
bindsym $mod+l scratchpad show

## Applications
set $launch exec --no-startup-id ~/.scripts/i3/launch_if_not_running.sh
bindsym $mod+h $hangouts scratchpad show
bindsym $mod+shift+d $launch discord || i3-msg '$discord scratchpad show'
bindsym $mod+d $launch GDrive   || i3-msg '$gdrive scratchpad show'
bindsym $mod+c $launch GCal     || i3-msg '$gcal scratchpad show'
bindsym $mod+k $launch GKeep    || i3-msg '$gkeep scratchpad show'
bindsym $mod+i $launch GInbox   || i3-msg '$inbox scratchpad show'

# Move workspaces to different displays
bindsym $mod+control+1 move workspace to output $side
bindsym $mod+control+2 move workspace to output $main
bindsym $mod+control+3 move workspace to output $aux

# Remove title bars
new_window 1pixel

# Add system menu
set $mode_power <span>     </span><span background='#fabd2f' foreground='#000000'> power </span> <span foreground='#FFFFFF'>l</span>ock <span foreground='#FFFFFF'>e</span>xit <span foreground='#FFFFFF'>s</span>uspend <span foreground='#FFFFFF'>h</span>ibernate <span foreground='#FFFFFF'>r</span>eboot <span foreground='#FFFFFF'>p</span>oweroff
bindsym $mod+0 mode "$mode_power"
mode --pango_markup "$mode_power" {
	bindsym l exec --no-startup-id i3exit lock, mode "default"
	bindsym e exec --no-startup-id i3exit logout, mode "default"
	bindsym s exec --no-startup-id i3exit suspend, mode "default"
	bindsym h exec --no-startup-id i3exit hibernate, mode "default"
	bindsym r exec --no-startup-id i3exit reboot, mode "default"
	bindsym p exec --no-startup-id i3exit shutdown, mode "default"

	bindsym Return mode "default"
	bindsym Escape mode "default"
}

# bar {
# 	status_command i3blocks
# 	tray_output primary
# 	colors {
# 		separator #888888
# 		background $transparent
# 		statusline $white
# 		focused_workspace $green $green $white
# 		active_workspace $transparent $transparent $white
# 		inactive_workspace $transparent $transparent #888888
# 		urgent_workspace $transparent $transparent $white
# 		#urgent_workspace #C20000 #C20000 #FFFFFF
# 	}
# }

client.focused #4E9C00 #4E9C00 #FFFFFF #FFFFFF
client.focused_inactive #1B3600 #1B3600 #888888 #FFFFFF
client.unfocused #333333 #333333 #888888 #FFFFFF
client.urgent #C20000 #C20000 #FFFFFF #FFFFFF

# Autostart some applications
exec --no-startup-id xedgewarp -b
exec --no-startup-id conky --quiet -u 10 -d
exec --no-startup-id ~/.fehbg

# Auto-switch to the first workspace on startup
exec --no-startup-id i3-msg 'workspace $ws1'

# Dim the screen in
exec --no-startup-id xbacklight -time 1000 -set 50

# Add bars to the top and bottom
exec_always --no-startup-id $HOME/.scripts/i3/launch_polybar.sh
